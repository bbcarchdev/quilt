# Extends integration.yml.default; run with `docker-compose -f integration.yml.default -f development.yml.default ...`

quilt:
  build: ../../
  dockerfile: quilt/docker/Dockerfile-build
  privileged: true
  ports:
   - "80:80"
  volumes:
   - ../:/usr/local/src/quilt
   - ../../spindle:/usr/local/src/spindle
